<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ShareService/QuoteMaker write everything they hand to WhatsApp
         into the app's cache dir (via path_provider's temp directory). -->
    <cache-path name="har_din_cache" path="." />
</paths>
